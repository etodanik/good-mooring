--- a/Common_3/Game/ThirdParty/OpenSource/Jolt/Jolt/Physics/Collision/BroadPhase/QuadTree.cpp
+++ b/Common_3/Game/ThirdParty/OpenSource/Jolt/Jolt/Physics/Collision/BroadPhase/QuadTree.cpp
@@ -297,7 +297,11 @@
 #endif
 
 	// Create space for all body ID's
-	NodeID *node_ids = mNumBodies > 0? new NodeID [mNumBodies] : nullptr;
+	// Mooring PoC: the physics world is bounded to 256 bodies. Keep rebuild
+	// scratch on the stack so a moving vessel does not allocate every tick.
+	Array<NodeID, STLLocalAllocator<NodeID, 256>> node_ids_storage;
+	node_ids_storage.resize(mNumBodies);
+	NodeID *node_ids = node_ids_storage.data();
 	NodeID *cur_node_id = node_ids;
 
 	// Collect all bodies
@@ -386,9 +390,6 @@
 		uint32 root_idx = AllocateNode(false);
 		root_node_id = NodeID::sFromNodeIndex(root_idx);
 	}
-
-	// Delete temporary data
-	delete [] node_ids;
 
 	outUpdateState.mRootNodeID = root_node_id;
 }
